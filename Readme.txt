Creating a basic perceptron model which can classify between two class with high accuracy. Two datasets are created, one for simple linear model and other for more complex non-linear model which will require a deep layer for classification. 

requirements:-

pip install torch===1.5.0 torchvision===0.6.0 -f https://download.pytorch.org/whl/torch_stable.html